guardian: defender-class endpoint protection engine for WLTIOS

5-layer eBPF HIDS: YARA + eBPF inline enforcement + FIM + native C isolation
forest + Tor-focused network IDS. 43 BPF hooks, canary engine, ransomware
detection, PMU counters, BPF LSM hooks. Tamper-evident forensic log, signed
updates over Tor, kill switch.

Build: gcc 15.2 + clang-18, self-test passes with 3 regression-verified
integration tests (canary restart, iforest inference, ransomware rate).

Complete project: 5 phases, 9 bug-hunt fixes, 55 source files.
